# URL base do backend Spring Boot. Deixe como está para rodar só o frontend (modo demo).
# Em produção na Vercel, configure esta variável no painel do projeto (Settings > Environment Variables).
VITE_API_URL=http://localhost:8080
